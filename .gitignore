# Secrets / local env
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime artifacts
outputs/
persist/
.docker-persist/

# Local config written at runtime (kept out of git; persisted via volumes in Docker)
LLM/llm_config.json
LLM/mcp_config.json
data/datasource_config.json
